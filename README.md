# hello-agent
PIAIC Q2
Assignment #2

uv venv
uv pip install openai python-dotenv
uv init --name hello-agent

This assignment demonstrates "Hello, world!" agent using the OpenAI Agent SDK.

## How to Run

1. Cloned the repo with GitHub Desktop to tariqnasim/hello-agent
2. Created a `.env` file with your OpenAI API key:
3. Install the dependencies
4. created the agent_hello.py
5. Executed within VSCode Ctrl+Alt+N and from the terminal too.
6. Output is attached as a screenshot.
